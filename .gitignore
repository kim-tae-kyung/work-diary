# Diary data (personal records) — never commit with the code.
/diary/

# Runtime logs (regenerated by the LaunchAgent each run)
/logs/

# Local config (no secrets, but contains a personal path)
config.toml

# Python bytecode caches
__pycache__/
*.pyc

# macOS / editor cruft
.DS_Store
